{
    "name": "ts-web-client",
    "type": "module",
    "private": true,
    "scripts": {
        "serve": "bun server.ts",
        "dev": "bun server.ts",
        "type-check": "tsc --noEmit",
        "lint": "oxlint src/",
        "test:coverage": "bun test --coverage",
        "check": "bun run type-check && bun run lint && bun test"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "lit-html": "^3.3.3",
        "rxjs": "^7.8.2"
    }
}
