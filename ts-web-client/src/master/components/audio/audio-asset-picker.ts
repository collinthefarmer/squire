import { AudioAssetPickerClass } from "@shared/components/asset-picker";

/**
 * Audio asset picker component
 *
 * Dropdown populated from AssetService for selecting audio files.
 * Re-exports the pre-configured shared AssetPicker.
 */
export const AudioAssetPicker = AudioAssetPickerClass;
